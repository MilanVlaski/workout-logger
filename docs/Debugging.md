Only way to permanently kill a service worker is to do:
- In the same Application tab, click Storage and then click Clear site data. This wipes the Service Worker, the Cache Storage, and IndexedDB in one go.
